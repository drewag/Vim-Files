Installation:

    git clone git@github.com:drewag/Vim-Files.git ~/.vim
    ln -s ~/.vim/vimrc ~/.vimrc
    cd ~/.vim
    git submodule update --init
    cd ~/.vim/bundle/commant-t
    bundle install
    rake make
